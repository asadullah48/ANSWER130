import GenerationOptions from '../entities/GenerationOptions';
import MathFunction from './MathFunction';
export declare type LinearFunctionSubType = 'STANDARD' | 'POINT-SLOPE' | 'SLOPE-INTERCEPT';
export interface LinearGenerationOptions extends GenerationOptions {
    subType?: LinearFunctionSubType;
}
export declare class LinearFunction extends MathFunction {
    protected subType: LinearFunctionSubType;
    protected equations: string[];
    constructor({ subType, randRange, }?: LinearGenerationOptions);
    private generateSlopeInterceptVariances;
    private generatePointSlopeVariances;
    private generateStandardVariances;
}
